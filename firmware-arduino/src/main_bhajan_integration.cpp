/*
  main_bhajan_integration.cpp is an integration helper and should not be
  compiled into the firmware. Keep it in the repo for reference but exclude
  it from builds by wrapping the whole file in a compile-time guard.
*/

#if 0
// Integration helper content intentionally omitted.
#endif