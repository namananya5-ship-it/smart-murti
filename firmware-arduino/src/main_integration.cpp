/*
  The contents of this file are integration guidance and intentionally
  excluded from the build by wrapping in #if 0 so PlatformIO doesn't
  compile it as a duplicate of `main.cpp`.
*/
#if 0
#include "BhajanAudio.h"
// (integration content omitted)
#endif